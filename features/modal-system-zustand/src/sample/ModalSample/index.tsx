import type { ModalSampleProps } from '@/sample/ModalSample/ModalSample.types.ts';

const ModalSample: React.FC<ModalSampleProps> = ({ text }) => {
  return <div>{text}</div>;
};

export default ModalSample;
